import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default class Calendar extends React.Component {
    render () {
        return (
            <View>
                <Text style={{color:'#ffffff'}}>Calendar</Text>
            </View>
        )
    }
}